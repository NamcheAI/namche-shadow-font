---
"@namche/namche-shadow": minor
---

Add generated, framework-agnostic `@font-face` CSS entry points for all three families and each family individually. New `.cdn.css` entry points pin immutable CDN URLs to the npm package version, while the existing `.css` entry points remain fully self-hosted.
